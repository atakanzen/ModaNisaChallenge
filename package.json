{
  "name": "modanisachallenge",
  "version": "1.0.0",
  "description": "Assignment Project",
  "main": "server.js",
  "scripts": {
    "start": "node server",
    "server": "nodemon server",
    "client": "npm run serve --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "test": "NODE_ENV=test mocha --recursive --parallel --exit --timeout 60000"
  },
  "author": "Atakan Zengin",
  "license": "ISC",
  "devDependencies": {
    "colors": "^1.4.0",
    "concurrently": "^5.3.0",
    "mongodb-memory-server": "^6.6.7",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.4"
  },
  "dependencies": {
    "chai": "^4.2.0",
    "config": "^3.3.1",
    "express": "^4.17.1",
    "express-validator": "^6.6.1",
    "mocha": "^8.1.3",
    "mongoose": "^5.10.3",
    "supertest": "^4.0.2"
  }
}
